/**
 * Sentry client-side configuration.
 *
 * TODO: Install @sentry/nextjs before enabling:
 *   npm install @sentry/nextjs
 *   npx @sentry/wizard@latest -i nextjs
 *
 * NOTE: Run 'npx @sentry/wizard@latest -i nextjs' to complete Sentry setup in Vercel CI for source maps.
 *
 * After installing, uncomment the code below and delete this comment block.
 */

// import * as Sentry from "@sentry/nextjs";
// Sentry.init({
//   dsn: process.env.SENTRY_DSN,
//   tracesSampleRate: 0.1,
//   debug: false,
//   replaysOnErrorSampleRate: 1.0,
//   replaysSessionSampleRate: 0.1,
//   integrations: [],
// });
