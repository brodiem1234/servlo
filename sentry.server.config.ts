/**
 * Sentry server-side configuration.
 *
 * TODO: Install @sentry/nextjs before enabling:
 *   npm install @sentry/nextjs
 */

// import * as Sentry from "@sentry/nextjs";
// Sentry.init({
//   dsn: process.env.SENTRY_DSN,
//   tracesSampleRate: 0.1,
//   debug: false,
// });
